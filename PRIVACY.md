# プライバシーポリシー

**CertainlyYoutubeShorts は、いかなる情報も収集・送信しません。**

最終更新: 2026-08-16

## 収集しないもの

この拡張機能は、以下を含むあらゆる情報を収集しません。

- 個人を特定できる情報
- 閲覧履歴・視聴履歴
- 検索語句
- 認証情報・Cookie
- 位置情報
- 技術的な情報や利用状況

外部のサーバーへ送信する処理は一切ありません。開発者を含む第三者が、
この拡張機能を通じて利用者の情報を受け取ることはありません。

Firefox のマニフェストにも、データ収集が無いことを明示しています。

```json
"data_collection_permissions": { "required": ["none"] }
```

## 端末内に保存するもの

不具合の是正に必要な情報だけを、**利用者の端末の中だけ**に保存します。
いずれもタブを閉じると消える `sessionStorage` に置かれ、外部へは出ません。

| 保存先のキー | 内容 | 目的 |
| --- | --- | --- |
| `__cys_order__` | チャンネルの Shorts 一覧の並び（動画 ID の配列）と、選んでいた並び順 | クリック後の再生順を一覧どおりに保つため |
| `__cys_log__` | 動作記録（時刻・判断内容・URL のパス） | 不具合が起きたときに原因を追うため |
| `__cys_repaired__` | 直前に是正した動画 ID と時刻 | 同じ是正を繰り返して再読み込みが止まらなくなるのを防ぐため |

いずれも次の操作で消せます。

```javascript
document.dispatchEvent(new Event("cys-消去"))
```

## 通信について

この拡張機能が行う通信は、**YouTube 自身に対するものだけ**です。

チャンネルの Shorts 一覧の続きを取得するために `www.youtube.com` へ問い合わせます。
これは利用者がブラウザーで一覧をスクロールしたときと同じ経路・同じ相手であり、
第三者のサーバーは関与しません。

## 権限について

この拡張機能が要求する権限は、`www.youtube.com` 上でスクリプトを動かすことだけです。
他のサイトでは一切動作しません。

## 変更について

内容を変更した場合は、このファイルと拡張機能のバージョンを更新します。

## 連絡先

不具合の報告や問い合わせは GitHub のイシューでお願いします。
