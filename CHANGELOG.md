# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
版番号は [セマンティックバージョニング](https://semver.org/lang/ja/) に従う。

## [1.0.0] - 2026-08-16

最初の公開版。開発中の版番号（0.1.0〜2.0.0）を整理し、ここを起点とする。

### 追加

- **予防**: Shorts へのリンクのクリックを横取りし、通常のページ遷移で開く。
  不具合は SPA 遷移の経路でのみ発生するため、経路そのものを断つ。
- **順送り**: チャンネル一覧でクリックした時点の並び順（新しい順／人気の動画／古い順）を記憶し、
  ホイールと `ArrowDown` / `ArrowUp` を横取りしてその順で遷移する。
  描画済みの分を使い切ったらチャンネルページを取り直して一覧を延長する。
- **是正（別動画）**: 着地から 4 秒以内にユーザー操作なしで別の ID へ移った場合、
  400ms 待って自力で戻らなければフルロードで引き戻す。
- **是正（黒画面）**: 再生中の映像が表示領域から完全に外れていたら中央に引き戻す。
- **診断**: 判断の過程を `sessionStorage` に記録する。
  `document.dispatchEvent(new Event("cys-画面"))` などで取り出せる。

### 技術的な決定

- コンテンツスクリプトは**隔離環境**で動かす。
  YouTube の CSP に `strict-dynamic` が含まれるため、
  `<script src="moz-extension://...">` をページに差し込む方式は弾かれる。
  `world: "MAIN"` も環境によっては効かないため、隔離環境だけで完結する構成にした。
